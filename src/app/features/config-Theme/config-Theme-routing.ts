import { Routes } from '@angular/router';
import { ConfigThemeComponent } from './config-Theme';

const configThemeRoutes: Routes = [
    { path: '', component: ConfigThemeComponent }
];

export default configThemeRoutes;